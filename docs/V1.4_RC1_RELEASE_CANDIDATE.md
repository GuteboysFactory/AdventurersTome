# Adventurer's Tome v1.4.0-rc.1 — Tome GM Dock Release Candidate

**Foundry verified runtime:** V13.351  
**Architecture policy:** v14-first, v13-compatible  
**Feature state:** FROZEN  
**System policy:** system-independent Core; external Dock Hosts are optional capability providers.

## Purpose

v1.4.0-rc.1 is the first release candidate for the completed Tome GM Dock milestone.

No new feature scope is introduced. RC testing is limited to regression, packaging, lifecycle, privacy, compatibility and release-readiness issues.

## Verified QA carried into RC

The following gates were completed on the qa.9 line:

- runtime release gate healthy
- v1.3 convergence healthy and structurally healthy
- takeover misses = 0
- public API composition intact
- reload / rename / delete / stale-context lifecycle healthy
- hosted provider integration healthy
- standalone fallback healthy
- GM/private boundaries healthy
- multi-client smoke healthy
- explicit Quick Capture target behavior healthy
- console-clean smoke healthy
- core Tome feature smoke healthy

## RC smoke

After installing rc.1:

- [ ] Tome starts without recurring console errors
- [ ] `game.modules.get("adventurers-tome")?.api?.gmDock?.releaseGate?.().healthy === true`
- [ ] standalone Dock works when no compatible host is present
- [ ] hosted-provider mode works when a compatible `gbf-gm-dock-host` is present
- [ ] no duplicate Dock/provider appears
- [ ] Quick Capture works
- [ ] Private Vault works
- [ ] Reveal Queue works
- [ ] Next Session / GM Dashboard works
- [ ] pin/unpin/current context works
- [ ] Home / Sessions / Quests / Group / World / Rules / Search open normally
- [ ] player clients do not receive GM-only Dock or Private Vault content

## Freeze rule

During rc.1:

- blocker or release-readiness defects may be patched
- no new feature is added
- no canonical data model is changed
- no host contract is expanded unless required to fix a blocker
- reference-system versions remain non-binding

## Stable promotion criteria

Promote rc.1 to `v1.4.0` when:

1. RC install/update path is clean,
2. runtime release gate remains healthy,
3. no blocker is found in real play,
4. console remains clean,
5. standalone and optional hosted operation remain healthy,
6. no v1.3 source-convergence regression appears.

## Result

**Status:** [ ] PASS  [ ] FAIL  [ ] BLOCKED  
**Approved for v1.4.0 stable:** [ ] YES  [ ] NO
